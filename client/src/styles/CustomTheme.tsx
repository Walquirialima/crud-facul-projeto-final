import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#a31da3",
    },
    secondary: {
      main: "#038103",
    },
  },
});

export default theme;
